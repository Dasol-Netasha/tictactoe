import 'package:flutter/material.dart';

class SquareMark extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 24,
      height: 24,
      color: Colors.black,
    );
  }
}